# Fullsend: data-room-drive-export
Base branch: dev
Worktree: .worktrees/fullsend-data-room-drive-export   Branch: fullsend/data-room-drive-export
Source: docs/ideas/data-room-drive-export.md

- [x] plan    → docs/plans/2026-06-11-data-room-drive-export.md (auto-review: 1 round; fixed 6 real issues — spec import order, unmocked logRepo.save, exact openapi $ref string, safer picker folder-select form over ViewId.FOLDERS, single-factory jest.mock edits, concrete a11y labels for the multi-select tests)
- [x] execute → 9 commits (487acdf..540b753); 703 backend + 106 frontend tests green; builds + tsc + npm run gen all clean; opus final review = SHIP (no blockers); applied 1 nice-to-have (controller delegation test for import/export symmetry).
- [x] qa     → GREEN to the max autonomous boundary. Ran worktree FE (next dev on 3001, reusing per-origin Firebase auth) + worktree BE (8081, local-disk). Verified in the REAL browser: per-file "Save to Google Drive" action + the "Select <name>" multi-select checkbox + the "N selected / Save N to Google Drive / Clear selection" bulk bar all render; multi-select toggles correctly; NO folder-UI/import/download/move/delete regression. Clicking export → ZERO JS errors → launched the correct Google OAuth flow (new tab, client_id 478977396808-…, scope EXACTLY `drive.file`, origin localhost:3001) — confirms button→handleExportToDrive→pickDriveFolderForExport→GIS token client→OAuth wiring + scope. Backend log: 0 errors. HUMAN-GATED (not autonomously testable): completing Google sign-in → folder pick → real drive.files.create write → `export` audit row (the idea doc's flagged "folder-select under drive.file" assumption). Low-risk spot-check post-merge — it's a near-exact mirror of the already-working import flow.
- [x] merge  → fast-forwarded dev 34fde52→540b753 (local, no push). Worktree removed, branch deleted. Main backend already serving merged code on 8081 (export route returns 401 not 404 → present); main frontend restarted on 3001. RUN COMPLETE.
- [x] LIVE HAPPY-PATH VALIDATED (post-merge, operator reauthed gcloud) → On the merged main servers: uploaded a CSV (GCS upload now works — object_key shows a real GCS path), operator completed the Google drive.file OAuth + folder pick in the browser, and the export fired successfully. Confirmed by: (a) `export` audit row written for the file by the correct user (the row is logged ONLY after a successful drive.files.create), (b) UI cleared the selection + showed no error. Then deleted it — full custody chain on one file: upload→export→delete, all logged. The idea doc's flagged "weakest assumption" (folder-select under drive.file) is now PROVEN live, end-to-end.

## Decision log (additional)
- 2026-06-11 (qa env): Reused feature-1's GCS→local-disk workaround (dev GCS creds still expired). Export reads from storage + writes to Drive; the Drive-write auth is the browser drive.file token (independent of storage creds), so the only true happy-path blocker is the Google OAuth, which needs a human.
- 2026-06-11 (qa env): Frontend has changes this time, so swapped BOTH 8081 (worktree BE) and 3001 (worktree FE, `next dev`) — the per-origin Firebase auth on localhost:3001 persisted across the swap, avoiding a re-login. Restored both to main post-merge.
- 2026-06-11 (env obs): The operator's main backend on 8081 appears to be under a supervisor that auto-restarts `node dist/main` when the port frees (observed twice, at both merges). After each merge + rebuild it came back on its own with the new code — so manual restart wasn't needed for the backend (only the frontend).
- 2026-06-11 (operator action needed): dev/main backend uses GCS for data-room storage but its Google Cloud creds are expired (invalid_rapt). Run `gcloud auth application-default login` to restore data-room uploads/downloads AND the export happy-path on the main backend. This was already broken before this run.

## Decision log
- 2026-06-11: Run 2 of 2 under `/loop /fullsend across docs/ideas`. Worktree off dev@34fde52 (which already includes the merged agent-data-room-tools feature). node_modules symlinked, env copied. This feature has BOTH frontend (Save-to-Drive action + folder Picker) and backend (export endpoint) changes — unlike feature 1.
- 2026-06-11: Known QA-env constraint carried over from feature 1 — the dev GCS creds are expired (invalid_rapt). Export streams GCS→Drive, so it genuinely needs working GCS to test the happy path. Will assess at QA: either the operator reauths gcloud, or QA focuses on the wiring/role/custody/picker with GCS mocked or flagged as a known env gap.
