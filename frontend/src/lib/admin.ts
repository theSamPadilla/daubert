/**
 * Email domain that grants admin access.
 *
 * MUST be kept in sync with `backend/src/modules/auth/admin.constants.ts`.
 * If the domain ever changes, update both files in the same commit.
 */
export const ADMIN_EMAIL_DOMAIN = 'incite.ventures';
